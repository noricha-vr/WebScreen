version: '3'
services:
  web_screen:
    container_name: 'web_screen'
    image: 'web_screen'
    volumes:
      - .:/app
      - ./drivers:/root/.wdm/drivers/
    ports:
      - 8080:8080
    environment:
      - PORT=8080
      - ALLOW_HOST=0.0.0.0
      - BUCKET_NAME=vrchat
#    networks:
#      - my_network
#networks:
#  my_network:
#    external: true